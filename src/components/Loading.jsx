import "../styles/loading.sass";

export default function Loading() {
    return (
        <main>
            <div class="loader">Loading
                <span></span>
            </div>
        </main>
    );
}
